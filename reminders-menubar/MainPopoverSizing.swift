import Foundation

enum MainPopoverSizing {
    static let defaultSize = NSSize(width: 340, height: 460)
    static let minSize = NSSize(width: 300, height: 320)
    static let maxSize = NSSize(width: 600, height: 1_000)
    static let minWidthPadding: CGFloat = 80
    static let minHeightPadding: CGFloat = 120
}
